using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Right_w_rot : MonoBehaviour
{
    //public _Rotation rot_r;
    //public float eil_rot;
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log(rot_r.Rot_eil_right);
        //transform.rotation = Quaternion.Euler(50, 0, 0);
        //transform.rotation = Quaternion.Euler(Main_Scene.Rot_eil_right, 0, 0);
        
    }

    // Update is called once per frame
    void Update(){
       // transform.rotation = Quaternion.Euler(70, 0, 0);
        

    
        //rot_r.Rot_eil_right = eil_rot;
        //Right_w_rot.transform.rotation = Quaternion.Euler(50, 0, 0);
        
    }
}
