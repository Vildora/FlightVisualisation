using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Left_w_rot : MonoBehaviour
{
   //public _Rotation rot_l;
    // Start is called before the first frame update
    void Start()
    {
        
        //transform.rotation = Quaternion.Euler(50, 0, 0);
        
        
    }

    // Update is called once per frame
    void Update()
    {
       // Debug.Log(rot_l.Rot_eil_left);
        //transform.rotation = Quaternion.Euler(rot_l.Rot_eil_left, 0, 0);
        
    }
}
